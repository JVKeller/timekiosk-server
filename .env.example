# TimeKiosk Server Configuration
# Copy this file to .env and adjust values for your deployment

# =============================================================================
# Server Configuration
# =============================================================================
PORT=3333
HOST=0.0.0.0

# =============================================================================
# TLS Settings
# =============================================================================
# When running behind a reverse proxy (like Nginx Proxy Manager), set ENABLE_TLS=false
# The reverse proxy handles SSL termination and forwards HTTP to this server.
# When running standalone with direct HTTPS access, set ENABLE_TLS=true
ENABLE_TLS=false
TLS_CERT=./certs/cert.pem
TLS_KEY=./certs/key.pem

# =============================================================================
# CORS Configuration
# =============================================================================
# Allowed origins for cross-origin requests
# Options:
#   * = Allow all origins (default, good for development)
#   https://example.com = Single specific origin
#   https://example.com,https://app.example.com = Comma-separated list
CORS_ORIGIN=*

# =============================================================================
# Authentication
# =============================================================================
# Token required for API access (shared secret between server and clients)
# Must be at least 8 characters
SYNC_TOKEN=your-secure-token-here

# =============================================================================
# Database
# =============================================================================
# PostgreSQL connection string
DATABASE_URL=postgres://timekiosk_user:your-password@localhost:5432/timekiosk
